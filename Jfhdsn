#include <stdio.h>
#include <stdbool.h>

int main(void)
{
  /*
  char *str = "547 042  793 a81 66";
  char *pstr = str;
  bool mute = false;
  while (*pstr)
  {
    if (*pstr == ' ')
    {
      if (!mute)
        putchar('\n');
      mute = true;
    }
    else
    {
      putchar(*pstr);
      mute = false;
    }
    pstr++;
  }

  */

  char type = "kot";
  char name = "pimpek";
  int kg = 13;
  char kolor = "czarny";
}

char *dzwiek(char *zwierz)
{
  char kot[] = "kot";
  if (strlen(zwierz) == strlen(kot))
  {
    if (memcmp(zwierz, kot, strlen(kot)) == 0)
    {
      print("miau");
    }
  }
}
